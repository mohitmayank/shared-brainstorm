node_modules/
dist/
coverage/
.env
.env.local
.DS_Store
*.log
*.pid
.vite/
packages/*/dist/
packages/server/dist/public/
~/.shared-brainstorm/  # never write here from repo
.superhuman/
.claude/settings.local.json
*.tsbuildinfo
docs
tasks
.planning

# Build artifacts copied into packages/server/ by scripts/prepack.mjs.
# These are reassembled on every `npm publish`; tracking them in git would
# produce churn and confusing diffs.
packages/server/skills/
packages/server/README.md
packages/server/LICENSE

# Local agent instructions (not tracked)
CLAUDE.md

# Playwright E2E artifacts
test-results/
e2e/test-results/
e2e/playwright-report/
e2e/.tmp/

# Playwright MCP scratch output (console logs, page dumps)
.playwright-mcp/

# Stray scratch screenshots
check-s2.png

# Marketing drafts (local only, not part of the shipped project)
marketing/
